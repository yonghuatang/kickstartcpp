#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main() {
    int t;
    cin >> t;
    // DECLARE AND READ VARIABLES HERE
    for (int tc=1; tc<=t; tc++) {
        int n;
        cin >> n;
        vector<ll> arr;
        for (auto i: arr) {  // &?
            cin >> i;
        }
        int ans;
        if (n == 2) {
            ans = 2;
        } else {

        }
        cout << "Case #" << tc << ": " << ans << '\n';
    }
    return 0;
}
