#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const int MOD = 1000000007;

int main() {
    int t;
    cin >> t;
    for (int tc=1; tc<=t; tc++) {
        int n;
        cin >> n;
        for (int i=0; i<n; i++) {
            int si, ei, li;
            cin >> si >> ei >> li;
            
        }
        cout << "Case #" << tc << ": " << 000000 << '\n';
    }
    return 0;
}
