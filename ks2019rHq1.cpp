#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const int MOD = 1000000007;

int main() {
    int t;
    cin >> t;
    for (int tc=1; tc<=t; tc++) {
        int n, ans = 0;
        cin >> n;
        for (int i=i; i<=n; i++) {
            int ai;
            cin >> ai;
            ans = min(i, );
        }
        cout << "Case #" << tc << ": " << ans << '\n';
    }
    return 0;
}  // ??
